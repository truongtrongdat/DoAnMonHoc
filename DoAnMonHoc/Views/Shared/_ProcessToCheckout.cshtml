@model IEnumerable<DoAnMonHoc.ViewModel.CardVM>
<div class="card-body">
	<div class="d-flex justify-content-between mb-3 pt-1">
		<h6 class="font-weight-medium">Tổng đơn giá</h6>
		<h6 class="font-weight-medium">@Model.Sum(p => p.tongTien)VNĐ</h6>
	</div>
	<div class="d-flex justify-content-between">
		<h6 class="font-weight-medium">Phí vận chuyển</h6>
		<h6 class="font-weight-medium">30000VNĐ</h6>
	</div>
</div>
<div class="card-footer border-secondary bg-transparent">
	<div class="d-flex justify-content-between mt-2">
		<h5 class="font-weight-bold">Tổng Tiền</h5>
		<h5 class="font-weight-bold">@(Model.Sum(p => p.tongTien) + 30000)VNĐ</h5>
	</div>
	<form asp-action="Checkout" asp-controller="Cart">
		<button class="btn btn-block btn-primary my-3 py-3">Xác nhận</button>
	</form>
</div>